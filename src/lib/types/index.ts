export type Feature = {
  id: number;
  title: string;
  description: string;
};

export type ReviewData = {
  id: number;
  image: string;
  name: string;
  review: string;
};

export type Link = {
  id: number;
  icon: string;
  link: string;
  name: string;
};
